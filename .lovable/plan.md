

Replace the SVG teddy bear in `TeddyBearLoader.tsx` with the uploaded Pudgy Penguin GIF. Steps:

1. Copy `user-uploads://Work_Out_Fitness_GIF_by_Pudgy_Penguins.gif` to `src/assets/pushup-penguin.gif`
2. Update `TeddyBearLoader.tsx` to import and display the GIF instead of the SVG bear, keeping the surrounding text and progress dots.

